# تَوَافُق — سجلّ التطوير التقني (Face Engine + Demo)

> **الغرض:** مرجع كامل لكل اللي اتعمل في محرّك الوجه والديمو، عشان نرجع له وقت بناء الأبلكيشن الحقيقي.
> **آخر تحديث:** 2026-08-06 · **القاعدة:** الملف ده بيتحدّث مع كل تطوير جديد.

---

## 1. بنية المشروع (النسخة المنشورة — الفولدر ده)

| ملف | الدور |
|---|---|
| `index.html` + `app.css` + `app.js` | **نسخة الأبلكيشن** (Cinematic UI) — اللي العميل يجرّبها |
| `lab.html` + `facelab.css` + `facelab.js` | **Face Lab** — نسخة الاختبار التفصيلية (كل القياسات + تصدير JSON) |
| `demo.html` + `demo.css` + `demo.js` | الرحلة الكاملة القديمة (73 سؤال توافق + أسئلة تكيّفية) |
| `face.js` | محرّك القياس: MediaPipe → 38+ قياس |
| `segment.js` | تقطيع الشعر (خط الشعر، قمة الأرملة، شعر على الجبهة) |
| `scoring.js` | محرّك الحساب: z-scores، الصفات، التوافق، إطار الوجه |
| `data/` | كل الداتا (أسئلة، صفات، baselines، إطارات) |
| `models/` | موديلات MediaPipe (landmarker + segmenter) |

**قاعدة الكاش المهمة:** كل تعديل في JS/CSS لازم يرفع رقم `?v=` في `index.html` و`lab.html` — وإلا الموبايلات هتفضل شغّالة بالنسخة القديمة (حصلت وسببت error فعليًا).

---

## 2. إصلاحات المحرّك الجوهرية (P0)

المحرّك الأصلي كان فيه 3 أخطاء بتضرب الدقة:

1. **قياسات بكسل خام + baseline واحد للكل** — قياسات زي `face_height` (بالبكسل) كانت بتتعاير بـ mean=0.3/std=0.2 الثابتين → z هائل → بتتقفل على ±1 لكل الناس (إشارة ثابتة كاذبة). **الحل:** baseline لكل قياس لوحده + `tanh(z/1.8)` بدل القصّ الحاد + القياسات اللي ملهاش baseline **بتتشال** بدل ما تدّي إشارة غلط.
2. **بروز الذقن الأمامي كان ضوضاء** — `|chin.x - lip.x|` في وش أمامي متماثل ≈ صفر + ضوضاء. **الحل:** alias `chin_projection_ratio` → `chin_sagittal_projection` (من صور البروفايل).
3. **ميل الرأس بيلوّث القياسات** — **الحل:** `rotateFace()` بيلف الصورة والـ landmarks مع بعض قبل القياس.

إضافات: `forehead_width_ratio` (نسبة حقيقية بدل `forehead_top_width` بالبكسل)، عتبات البكسل (كثافة حاجب/خطوط جبهة) بقت **تتكيّف مع إضاءة الوش نفسه**، وخط الشعر من الـ **Segmenter** مع fallback لمسح لون الجلد.

---

## 3. الـ Baselines (تطوّرها بالترتيب)

| مرحلة | المصدر | ملاحظات |
|---|---|---|
| ~~أول نسخة~~ | 282 عيّنة من صور الكورس | اكتشفنا تلوث: 52 غير أمامي + عيّنات متعلّمة غلط + ~نص الصور كرتون |
| ~~تانية~~ | 209 أمامي نضيف (بعد الفلترة) | أحسن، بس لسه فيها كرتون |
| **الحالية** | **FairFace — 1,033 وش حقيقي** (617 ذكر / 416 أنثى) | رخصة CC BY 4.0 (تجاري ✓)، نفس فلاتر التطبيق (أمامي ±8° + تعبير محايد) |

- الملف: `data/metric_baselines.json` — بنية `{all, male, female}` لكل قسم 27 قياس حقيقي (median + IQR-std + n).
- **الفروق بين الجنسين حقيقية:** widening_ratio يختلف 0.51σ — عشان كده شاشة البداية بتسأل ذكر/أنثى والـ z بيتعاير على جنس المستخدم (`zScoreMetrics(raw, gender)`).
- لسه **provisional** (تقديري): قياسات البكسل (brow_density، cheek_fullness، خطوط الجبهة) + قياسات البروفايل الخمسة.
- **مصدر تحسين مستمر:** زر تصدير JSON في الـ Lab — كل اختبار حقيقي = عيّنة معايرة جديدة.
- **backlog:** baselines شرق أوسطية + عمرية من الـ train split الكامل (~87 ألف وش) — سكربت الاستخراج في `scripts/`.

---

## 4. الصفات (نموذج الـ60) والتركيز على 15

- الموديل: **60 صفة** (hadi_01…60) × 3 أسئلة = 180 سؤال. الملفات: `traits_meta_60`, `question_bank_60`, `trait_question_matrix_60`, `trait_feature_map_60`, `plans_60`.
- **الرقم "15 صفة" القديم = موديل sabti قديم مش مستخدم** (`trait_feature_map.json` القديم — الديمو مش بيحمّله).
- **FOCUS_15** (في `app.js`): الـ15 الأقوى قياسًا من الوش (5-6 قياسات حقيقية لكل واحدة): الاجتماعية، الثقة في الآخرين، التسامح، النقد، الغيرة، تحمل المسؤولية، الطموح، إنجاز الأهداف، الصراحة، السرية، الكرم، العملية، حب الرفاهية، الإقناع، التفاؤل.
- كل كارت صفة فيه قسم **"ليه طلعت النتيجة دي؟"**: كل قياس مساهم + اتجاهه + قوته (`getTraitFeatures` + `whyRows`).
- **الاستدلال وش→صفة تلميح ضعيف بالتصميم:** `INFER_STRENGTH=0.5`، الثقة من تراكم أدلة (`EVIDENCE=2.0`)، skip الأسئلة عند ثقة ≥0.55 (greedy set-cover في `selectAdaptiveQuestions`).

---

## 5. إطار الوجه (Face Frames)

- مصنّف **nearest-centroid** في فضاء z على 6 قياسات هندسية، متعلّم من **1,265 وش متلبّل** (Kaggle face-shape عبر مرآة HF `codernotme/face_shape`).
- الدقة (holdout): **top-1 = 38.7%، top-2 = 61.7%** (الصدفة 20%). المربع 65% والقلب 55% الأقوى؛ بيضاوي/مستدير/مستطيل بيتداخلوا (ليبلات الداتاسيت متأثرة بالشعر).
- عشان كده الإخراج **soft**: لو الفرق واضح (margin ≥ 0.25) → إطار واحد؛ لو لأ → "بين إطارين".
- الملف: `data/face_frames.json` · الدالة: `Scoring.classifyFaceFrame(z)` · الكارت أول النتايج.
- **مش بنغذّي الصفات بالإطار لسه** — مستنيين دقة أعلى (موديل CNN جاهز 85%: `fahd9999/face_shape_classification` — يحتاج تحويل للمتصفح).
- قيد: داتا المعايرة ستات مشاهير فقط.

---

## 6. تدفق الالتقاط (النسخة المعتمدة — 2026-08-06)

> **معتمدة من يوسف** بعد مراجعة mockup (`flow_preview.html` — محاكاة بدون كاميرا للعرض).
> **الصوت اتلغى بالكامل** (كان Web Speech — اتشال بطلب صريح).

**التدفق صفحتين منفصلتين:**

**📄 صفحة 1 — «فحص الجاهزية» (`#s-check`):** كاميرا معاينة صغيرة + قايمة 5 بنود بتتعلّم
✓/✗ لحظيًا: `💡 الإضاءة · 📏 المسافة · 🕶 بدون نضّارة · 📱 مستوى العين · 😐 تعبير محايد`
(+ بند الشعر كتحذير غير مانع). البند الفاشل بيعرض تحته التعليمة. لما الخمسة يثبتوا متظبطين
**700ms متواصلة** → زر «التالي — التصوير ✓» بينوّر.

**📄 صفحة 2 — التقاط بأسلوب Face ID (2026-08-08، بديل نظام الوقفات):** **دايرة 36 شرطة (ticks)**
حوالين الوش بترسم على الكانفاس. التسلسل: burst أمامي سريع (250ms استقرار بس) → «لِف راسك
يمين ببطء» → «دلوقتي شمال». **الالتقاط بيحصل أثناء الحركة**: الشُرط بتخضرّ مع كل زاوية
اتغطت (`tickYaw = 75·cos(φ)` — اللفة الأفقية بتملا الدايرة كلها)، والمحرّك بيحتفظ بـ**أنضف
frame لكل جانب** (score = حدة الصورة − بُعد عن 56°؛ اكتمال الجانب عند |yaw|≥48). مفيش
hold-still ولا حلقة انتظار. فحص مركزية `centerCheck()` (قرّب/ابعد/في النص) + تعبير محايد
للأمامية بس. زر «التقاط يدوي» fallback بيملا المنطقة الناقصة حسب آخر yaw.

**الكواشف (مشتركة بين الصفحتين):**
- إضاءة: سطوع + ظل جانبي + **نور محروق على الوش** (bright_ratio > 6%)
- نضّارة: heuristic حواف (جسر الأنف + إطار تحت العين) — عتبات في `glassesCheck` بـ `face.js`
- شعر على الجبهة: Segmenter كل 0.7 ثانية (تحذير غير مانع — للحجاب/الغرّة)
- تعبير محايد: blendshapes (jawOpen>0.25 / smile>0.4 / blink>0.5)
- **مستوى العين (pitch):** من **الـ transformation matrix** (زوايا حقيقية) — "ارفع/نزّل الموبايل لمستوى عينك"، حد ±14°
- **ثبات (نمط BioID):** حركة الـ landmarks بين الفريمات / المسافة بين العينين — عتبة 0.006؛ التقاط بعد 800ms سكون
- **Portrait إجباري:** شاشة "دوّر الموبايل عمودي" لو landscape

**الالتقاط نفسه:** الأمامية burst **3 فريمات** والقياس بالوسيط (median) — بيقتل ضوضاء الفريم الواحد.

---

## 7. محرّك التوافق (للرجوع وقت بناء الـ matching الحقيقي)

- في `scoring.js → matchingScore()`: فلاتر صلبة (دين، أطفال، عمر، تعدد، صحة) ثم buckets مرجّحة (قيم 0.33 / تعلق 0.24 / شخصية 0.20 / حميمية 0.12 / وجه 0.06 / لوجستيات 0.05) + خصم الأنماط الخفية (حتى −0.2).
- ⚠ **`matching.js` ملف محرّك تاني كامل غير موصول** (index القديم مش بيحمّله) — فيه `buildProfile/compareProfiles` بمصفوفة التعلق وGottman — راجعه وقت بناء الـ matching الحقيقي وقرر: دمج ولا حذف.
- الشريك في الديمو **افتراضي** (نسخة من المستخدم + noise).

---

## 8. سكربتات الـ pipeline (فولدر `scripts/` في الريبو — مش في النشر)

| سكربت | بيعمل إيه |
|---|---|
| `faceshape_calibrate.py` | يتعلّم بصمات إطارات الوجه من داتا متلبّلة → `data/face_frames.json` |
| `https_server.py` | سيرفر HTTPS محلي (بورت 8443) لتجربة الكاميرا من الموبايل على نفس الشبكة |

**بيئة التشغيل:** `python3 -m venv ffenv && pip install mediapipe pillow pyarrow huggingface_hub requests`
**سكربتات اتمسحت ومحتاجة إعادة كتابة لو لزمت** (منطقها موثّق هنا): استخراج FairFace (`fairface_extract.py` — فلترة أمامي+محايد وحساب الـ27 نسبة) وتجميعها (`fairface_aggregate.py` — median/IQR لكل جنس).

---

## 9. النشر (Hosting)

### ⚠ اكتشاف مهم (2026-08-06): `netlify.app` محجوب
الرفع على Netlify نجح لكن الدومين `*.netlify.app` نفسه **محجوب عند مزوّدي إنترنت مصريين**
(ERR_CONNECTION_REFUSED رغم إن الـ DNS سليم و`netlify.com` شغّال). التشخيص: TCP للـ IPs بتاعة
الاستضافة بيترفض — حجب على مستوى المزوّد، مش مشكلة في الموقع.

### ✅ النشر الحالي: GitHub Pages
- **اللينك الرسمي:** `https://youssefsafwat1223-spec.github.io/frasaaaaa/`
- المصدر: branch **`gh-pages`** في نفس الريبو (محتواه = فولدر `facelab_deploy` بالظبط)
- **التحديث:** من جوّه `facelab_deploy/`:
  `git add -A && git commit -m "..." && git push -f origin gh-pages` (فيه git repo متداخل جاهز جوّه الفولدر)
- البناء بياخد ~دقيقة بعد الـ push
- `github.io` متأكد إنه مفتوح على الشبكات اللي جرّبناها

### احتياطي
- Netlify deploy موجود برضه (`scintillating-empanada-584e9d.netlify.app`) — ممكن يشتغل على شبكات تانية
- بدائل لو github.io اتحجب يومًا: Cloudflare Pages / Vercel / Surge (اتفحصوا وكانوا واصلين)
- **افتكر:** أي تعديل JS/CSS = ارفع `?v=` قبل النشر

للتجربة المحلية من الموبايل: `python3 scripts/https_server.py` ثم `https://<IP-اللابتوب>:8443` (نفس الـ Wi-Fi، واقبل تحذير الشهادة).

---

## 10. Backlog — اللي ناقص للأبلكيشن الحقيقي

- [ ] **Backend**: حسابات + تخزين + matching حقيقي + شات + دفع (سكيما PostgreSQL جاهزة في `DATABASE_SCHEMA.sql` بالريبو الأم)
- [ ] baselines شرق أوسطية + شرائح عمرية (FairFace train split)
- [ ] baselines بروفايل حقيقية (من الصور الجانبية المرفوضة في فلتر الـ pose)
- [ ] قياسات البكسل من داتا حقيقية (لسه provisional)
- [ ] إطار الوجه: موديل أدق (CNN 85%) + تغذية الصفات بيه + معايرة رجالي
- [ ] التحقق من إشارة الـ pitch على أجهزة مختلفة (لو "ارفع/نزّل" معكوسين — اعكس في `pitchIssue`)
- [ ] معايرة كاشف النضّارة على كاميرات موبايل مختلفة (عتبات `glassesCheck`)
- [ ] مراجعة تحريرية للـ180 سؤال + الـ73 سؤال
- [ ] الـ 3D (MICA/DECA على سيرفر) → يفتح الودان + الصفات الـ5 المقفولة (الوفاء، الاستقرار الأسري، الحكمة، الإبداع، حب الحرية)
- [ ] جمع اختبارات حقيقية (زر التصدير) → إعادة بناء baselines بشرية 100%

---

## 11. إعادة البناء النظيفة (2026-08-08)

بطلب من يوسف: **فولدر جديد خالص `tawafuq/`** بدل التعديل المتراكم على القديم —
`index.html` و`style.css` اتكتبوا من الصفر في مرة واحدة، و`app.js` نسخة نهائية نظيفة
(تدفق: ترحيب+نوع → فحص الجاهزية → التقاط Face-ID بالدايرة → معالجة → نتايج).
المحرّك (`face.js`/`segment.js`/`scoring.js`) والداتا والموديلات اتنقلوا زي ما هم
(مُختبَرين). الفولدرات القديمة (`demo/`, `facelab_deploy/`) بقت أرشيف — **الشغل كله
من دلوقتي في `tawafuq/` بس.** الـ Lab متاح على `lab.html` جوّه الفولدر.

**تحديث 2026-08-09 (v3) — Face-ID طبق الأصل:** خلفية بيضا والكاميرا في دايرة (مثل شاشة
أبل)؛ 72 شرطة رمادي فاتح `#D1D1D6`؛ **مروحة الشُرط بتتبع اتجاه الراس live وتستطيل**
(gaussian حول الـ yaw الحالي) قبل التسجيل؛ المتسجّل بيثبت طويل **بأخضر iOS `#34C759`**؛
النص غامق تحت الدايرة مباشرة؛ لحظة "اكتمل المسح ✓" (الدايرة كلها بتخضرّ 900ms) قبل المعالجة.
شيلنا: الخلفية الخضرا من شاشة المسح، فقاعة السهم، أي رسم على الوش.

**تحديث v4 (نفس اليوم) — إيقاع أبل البطيء:** الاكتمال بقى مشروط بتغطية **القوس كامل
(28°→62°) لكل جانب**، والتغطية مش بتتسجّل غير لو سرعة اللفة < ‎~70°/ث (أسرع من كده →
"بشويش… حرّك رأسك أبطأ"). النتيجة: المسح بياخد ~10-15 ثانية متأنية زي iPhone بدل لفتين سريعين.

**تحديث v5 — رأس إرشادي متحرك:** أيقونة رأس SVG تحت الدايرة بتوضّح الحركة المطلوبة:
قدّام ثابتة → في مرحلة اليمين بتلف يمين بشكل مستمر مع سهم أخضر منحني → الشمال معكوسة.
كلاسات `pg-front/pg-right/pg-left` بتتظبط من `setGuide()` حسب المرحلة.

**تحديث v6 — بروفايل كامل للجناب (مرجع صورة يوسف):** اللقطة الجانبية بقت "صيد أعمق زاوية":
الscore بيفضّل عمق الـ yaw بقوة (`|yaw|·1.2 + sharp·40`)، ومفيش سقف زاوية — بنتتبع لآخر ما
الموديل يستحمل (~75-85° عمليًا). الجانب بيتقفل لما المستخدم يوصل أعمق نقطة ويبدأ يرجع
(أو ≥80°) — فاللقطة المحفوظة هي الأعمق (بروفايل شبه كامل بالودن). ملاحظة: 90° الحرفية فوق
قدرة MediaPipe للتتبع — لو اتطلبت مستقبلًا محتاجة detection منفصل server-side.

**تحديث v7 — لقطة الـ90° + معاينة اللقطات:** (أ) BlazeFace detector (موديل محلي 230KB) كشبكة
أمان: لما الـ mesh يقع في عمق اللفة (≥50°) والـ detector لسه شايف الوش → بنحفظ لقطة العرض
الأعمق (display-only، مفيش قياس منها) مع رسالة "ده أعمق بروفايل 👌". (ب) كارت "لقطاتك
الثلاث" أول النتايج: يمين/أمامية/شمال جنب بعض زي الرندر المرجعي — الجانبية من display
(الأعمق) أو best كـ fallback.

**تحديث v8 — صور انستراكشنز:** شريط "هنصوّرك من ٣ زوايا كده" في شاشة الترحيب — 3 صور
مقصوصة من الرندر المرجعي بتاع يوسف (يمين/أمامية/شمال) في `img/pose_*.jpg`. **مرشّح الخطوة
الجاية (متفق عليه مبدئيًا):** تحويل موديل face-shape CNN (fahd9999، دقة 85%) للمتصفح
(ONNX→ort-web، ~15-20MB مضغوط) لرفع دقة كارت "إطار وجهك" من 39% — جلسة شغل مستقلة.

**تحديث v9 — موديل شكل الوجه CNN (2026-08-10):** EfficientNet (fahd9999، دقة مرجعية 85%)
اتحوّل fp16 ONNX (35MB، تطابق 20/20 مع الأصل على صور حقيقية؛ فحص سريع 20 صورة = 60% —
عيّنة صغيرة وليبلات مضروبة) وبيشتغل في المتصفح بـ onnxruntime-web. التكامل: (أ) كارت
"إطار وجهك" بيستخدم احتمالات الـCNN (fallback هندسي لو فشل التحميل)، (ب) الاحتمالات الخمسة
بتتحقن كقياسات `frame_*_score` (baseline مبدئي 0.2±0.25) و`face_squareness` اتوجّهت لـ
`frame_square_score` → القيادة/العملية/الإصرار/المسؤولية/إنجاز الأهداف بقوا بياخدوا إشارة
حقيقية من شكل الوجه. **أرقام الاعتماد على التصوير (محاكاة 3000 وش على المحرّك الفعلي):**
مفيش صفة 100%؛ Top-6 بتتحسم لـ~57% من الناس (الاجتماعية/الثقة/التسامح/النقد/الغيرة/الطموح)؛
10 صفات ~30%؛ الباقي أسئلة. التحويل scripts في scratchpad → انسخها لـ scripts/ لو هتعاد.

**تحديث v10 — شحن الموديل على أجزاء:** ضغط int8 (حتى static بمعايرة 60 صورة) **بيكسر دقة
EfficientNet** (6/20) — مرفوض. الشحنة النهائية: fp16 (35MB، مطابق 20/20) مقسوم **5 أجزاء
`face_shape_fp16.onnx.part0-4`** (عشان الرفع/التنزيل على النت البطيء)؛ التطبيق بيجمعهم
في الذاكرة وينشئ جلسة ort من الـ buffer. الملف الكامل في `.gitignore` (مش في الريبو).

**تحديث v12 — عمودية حقيقية + خط المناخير:** (أ) جيروسكوب (DeviceOrientation، بإذن iOS من
ضغطة "التالي") بيقيس ميل الموبايل الفعلي — beta بعيد عن 90°±30 → "خلّي الموبايل واقف عمودي"
ومفيش التقاط. (ب) في اللقطة الأمامية: **خط محور الوجه** (جبهة→أنف→ذقن) مرسوم live على خط
مرجعي متقطع — برتقاني لما مايل، **أخضر لما مستقيم** (yaw≤10 وroll≤8 وعمودية الجهاز) وساعتها
بس بيلقط. الـ debug overlay بقى بيعرض roll وbeta كمان.

---

## ✅ اعتماد النسخة للعرض على العملاء (2026-08-10)

يوسف جرّب v12 كاملة على موبايله واعتمدها: الفحوصات، مسح Face ID (خط المناخير + الجيروسكوب
+ الإيقاع البطيء + البروفايل العميق)، تحميل موديل الـ CNN المقسّم، النتايج (اللقطات الثلاث
+ إطار الوجه + الـ15 صفة + الثبات)، **ونسب اليمين/الشمال متطابقة ميدانيًا**. وضع الـ debug
(`?debug=1`) سايبينه للفحص المستقبلي. **الحالة: Client-ready.**
المرحلة الجاية المقترحة: الأسئلة (مراجعة الـ180 + تجربة الـ adaptive) أو baselines شرق أوسطية.
